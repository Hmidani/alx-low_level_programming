#include "main.h"

/**
 * set_string - a function that sets the value of a pointer to a char.
 * @s: the first string input.
 * @to: the second string input.
 *
 * Return: 0 Always (Success)
 */

void set_string(char **s, char *to)
{
	*s = to;
}

