#include "main.h"

/**
 * div - a function that retuns the division of 2 numbers..
 * @g: the first intger input.
 * @h: the second integer input.
 *
 * Return: division
 */

int div(int g, int h)
{
        return (g / h);
}
