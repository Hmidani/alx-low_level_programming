#include "main.h"

/**
 * mul - a function that retuns the multiplication of 2 numbers..
 * @e: the first intger input.
 * @f: the second integer input.
 *
 * Return: multiplication
 */

int mul(int e, int f)
{
        return (e * f);
}
