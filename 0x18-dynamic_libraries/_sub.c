#include "main.h"

/**
 * sub - a function that retuns the substraction of 2 numbers..
 * @c: the first intger input.
 * @d: the second integer input.
 *
 * Return: substraction
 */

int sub(int c, int d)
{
        return (c - d);
}
