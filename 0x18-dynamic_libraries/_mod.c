#include "main.h"

/**
 * mod - a function that retuns the rest of 2 numbers..
 * @a: the first intger input.
 * @b: the second integer input.
 *
 * Return: rest
 */

int mod(int a, int b)
{
	return (a % b);
}
