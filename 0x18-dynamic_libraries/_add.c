#include "main.h"

/**
 * add - a function that retuns the somme of 2 numbers..
 * @a: the first intger input.
 * @b: the second integer input.
 *
 * Return: somme
 */

int add(int a, int b)
{
	return (a + b);
}
